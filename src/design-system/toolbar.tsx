import * as React from 'react';
import { observer } from 'mobx-react';
import { Toolbar146D32 } from './toolbar.generated';

export const Toolbar = observer(props => {
  return <Toolbar146D32 {...props} />;
});
