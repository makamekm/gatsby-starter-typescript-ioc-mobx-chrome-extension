import * as React from 'react';
import { observer } from 'mobx-react';
import { TabBar146D0 } from './tab-bar.generated';

export const TabBar = observer(props => {
  return <TabBar146D0 {...props} />;
});
